models:
  - name: agg_top_artists_all_time
    columns:
      - name: artist_name
        description: "{{doc('artist_name')}}"
        meta:
          dimension:
            type: string
      - name: times_played
        description: "{{doc('times_played')}}"
        meta:
          dimension:
            type: number
