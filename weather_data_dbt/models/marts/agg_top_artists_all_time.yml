models:
  - name: agg_top_artists_all_time
    columns:
      - name: artist_name
        description: "{{doc('artist_name')}}"
        config:
          meta:
            dimension:
              type: string
      - name: times_played
        description: "{{doc('times_played')}}"
        config:
          meta:
            dimension:
              type: number